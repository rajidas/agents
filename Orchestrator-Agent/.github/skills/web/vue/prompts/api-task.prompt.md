# Vue.js API Task

Implement the requested API integration using `agents/vue-api.agent.md` and `skills/http.skill.md`. Inspect existing client, scripts, schemas, auth, and test conventions first. Keep API access typed and outside presentational components. Cover response validation, authentication, authorization, invalid input, errors, retries, and focused unit/integration tests using the compatible project runner.
