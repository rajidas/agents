# Vue.js Data Contracts

Define strict TypeScript domain models, DTOs, schemas, response mappings, pagination, filtering, sorting, and deterministic mock data. Validate untrusted API responses at transport boundaries and keep persistence behind services or repositories.

Do not expose database clients, private environment variables, or server-only authorization in Vue components. Document error contracts, publication/permission rules, indexes, and migration assumptions when backend work is in scope.
